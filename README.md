# 1000-Agent Indian Market Trading Bot
Advanced quantitative trading signal system using Google Gemini AI.

## Setup
1. Clone the repo.
2. `python -m venv venv && source venv/bin/activate`
3. `pip install -r requirements.txt`
4. Create `.env` based on `.env.example`.
5. `python main.py`
